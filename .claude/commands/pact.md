---
description: Compress this session with PACT and clear context
---

Summarize everything we've worked on in this session — the goal, files touched, what's done, what's next, constraints — into one dense paragraph. Then run:

```bash
pact compact "<your summary>"
```

Show the PACT output and the compression ratio.

Then tell the user:

"Context compressed. Copy the PACT output above, then run /clear. Paste it as your first message in the fresh session — the AI picks up exactly where you left off with 95% fewer tokens."
