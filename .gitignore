# Dependencies
node_modules/

# Build output
dist/

# PACT session state (per-project, generated by pact-cc install)
.pact/state.json

# Benchmark live results (pre-run canonical results ARE committed)
benchmarks/results/*.json
benchmarks/results/*.png

# Python build artifacts
python/dist/
python/*.egg-info/
__pycache__/
*.pyc

# macOS
.DS_Store

# Environment
.env
.env.local

# Local Claude Code session artifacts — not for public consumption
# (settings.json/local stay private; slash commands ship publicly so judges/users
#  get /pact and /pact-rehydrate the moment they clone the repo)
.claude/*
!.claude/commands/

# Hackathon prep / scratch — private working docs with old claims + judge prep
meta/
scratch/

# OS metadata
.DS_Store
